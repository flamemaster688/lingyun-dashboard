# 灵运 BI 看板 · 交接、问题诊断与部署协作指南

> 整理时间：2026-09-15 ｜ 整理人：接管方（新机器）
> 适用对象：3 人协作组（吴超 / 羽琪 / 赵莹）+ 接管方

---

## 〇、一句话结论（先读这个）

**看板本身没问题，在你电脑上现在就能开。** 它本质是一个**纯静态网站**（`static/`：HTML+CSS+JS+ECharts，无后端、无构建步骤）。
所谓"换电脑打不开"，根因**不是看板代码**，而是：

1. 之前靠 `deploy_netlify.sh`（写死了**赵莹 Windows 电脑的绝对路径**）来"开/部署"，换到 Mac 直接报错；
2. 协作方式是**传 zip 包**，交接即"断点"，没有版本控制，容易丢文件、踩路径。

所以正确打开方式 = 双击 `static/index.html`，或本地起个静态服务器即可。

---

## 一、已发现的问题清单（含严重度）

| # | 问题 | 严重度 | 说明 |
|---|------|--------|------|
| 1 | **硬编码 Windows 绝对路径** | 🔴 高 | `deploy_netlify.sh` 里 `NL="C:/Users/赵莹/..."`、`STATIC="C:/Users/赵莹/WorkBuddy/.../static"`；`COMMANDS.md` 示例也是 `C:\Users\赵莹\...`。在 Mac/Linux 上直接失效。 |
| 2 | **Netlify Token 明文硬编码** | 🔴 高（安全） | `deploy_netlify.sh` 第 5 行写了一个**可用的** `NETLIFY_AUTH_TOKEN=nfp_****`（活口令）。这是凭证泄露，必须轮换。 |
| 3 | **Netlify 账号额度已用尽** | 🟠 中 | 交接文档确认：新部署会被 403 拦截。旧的 Netlify 链接已失效，不能继续用。 |
| 4 | **靠 zip 传包协作，无版本控制** | 🟠 中 | 压缩包里**没有 `.git`**，三人改动靠"打包→回传→覆盖"极易冲突/丢改动，这正是交接痛点的根源。 |
| 5 | **仓库未"瘦身"** | 🟡 低 | 压缩包 117MB/841 文件，夹杂大量一次性诊断脚本（`diag_*.js`、`_step*.py`、`_raw_*.csv` 约百兆）、`.bak`、旧版本 `lingyun.app.v3/8/9.js`、调研图片等。 |
| 6 | **`data.js` 达 11MB 且依赖个人取数** | 🟡 低 | 真实数据快照内嵌在 `static/data.js`，刷新依赖赵莹的金山文档（kdocs）连接器；新接手方若没有该连接器，无法自助刷新数据。 |
| 7 | **个人虚拟环境被打包** | 🟡 低 | 压缩包内含 `C:/Users/赵莹/.workbuddy/...` 的 Python venv 路径（解压时未落盘，但增大了压缩包体积，也暴露了个人目录结构）。 |

---

## 二、已完成的修复（本次接管已做）

- ✅ **安全加固**：`deploy_netlify.sh` 已改为**必须从环境变量 `NETLIFY_AUTH_TOKEN` 读取**，不再落盘明文；缺失时直接报错退出。
  > ⚠️ 仍请**立即到 Netlify 后台吊销旧 Token 并重新生成**（Settings → Applications → 吊销）。
- ✅ **跨平台改造准备**：部署入口改为 GitHub Pages（见第四节），不再依赖 Windows 路径。
- ✅ **`.gitignore` 完善**：排除 `_raw_*`、`*.bak*`、`diag_*.js`、`*.png`、诊断/调研素材、`.worklify/`、`.workbuddy/`、个人路径（`赵莹/`、`C:/`、`Users/`）等，避免垃圾与凭证进库。
- ✅ **GitHub Pages 工作流修正**：`.github/workflows/deploy.yml` 已适配"仓库根=lingyun_dashboard"结构（去掉 `cd lingyun_dashboard`，产物路径改为 `dist`）。
- ✅ **本地可运行验证**：已用 `python -m http.server` 实测 `static/` 与 `dist/` 全部资源返回 200，确认本机可开。
- ✅ **Git 初始化**：已在 `lingyun_dashboard/` 初始化仓库并提交（95 个文件，自包含；含 `static/`、`build/`、`docs/`、`.github/`）。

---

## 三、在本机（Mac）怎么打开看板

任选其一：

```bash
# 方式 A：直接双击（最简单）
open lingyun_dashboard/static/index.html        # 或 dist/index.html（生产合并版）

# 方式 B：本地静态服务器（推荐，避免个别浏览器 file:// 限制）
cd lingyun_dashboard/static
python3 -m http.server 8080
# 浏览器打开 http://localhost:8080
```

> 当前我已在沙箱里把**生产版 `dist/`** 跑在 `http://127.0.0.1:8080`，可在 WorkBuddy 预览面板直接看。
> 注意：这只是本机/本会话预览，**分享给另外两位同事请用下面的 GitHub Pages 公网链接**。

---

## 四、怎么拿到"可分享的公网链接"（3 人共用）

**首选：GitHub Pages（免费、无额度上限、push 即自动部署）。** 项目里已配好 `.github/workflows/deploy.yml`，只要 push 到 GitHub 就自动出链接。

1. 在 GitHub 新建仓库，命名为 **`lingyun-dashboard`**（决定最终网址）。
2. 把本地仓库推上去（仓库根已是 `lingyun_dashboard`，直接推即可）：
   ```bash
   cd lingyun_dashboard
   git remote add origin https://github.com/<你的GitHub用户名>/lingyun-dashboard.git
   git branch -M main
   git push -u origin main
   ```
3. 仓库 **Settings → Pages → Source 选 "GitHub Actions"**。
4. 之后每次 `git push`（或 Actions 页手动 Run）都会自动 `node build/bundle.js` 并部署。
5. 最终链接：`https://<你的GitHub用户名>.github.io/lingyun-dashboard/`

**备选（不想开 GitHub）：** CloudStudio / Vercel / Cloudflare Pages 拖 `dist/` 即可（静态站点）。
> 本次尝试 CloudStudio 部署接口返回 400（需账号授权），未成功；如要用请在 WorkBuddy 里连好 CloudStudio 后重试，目录指向 `lingyun_dashboard/dist`。

---

## 五、3 人 Git 协作规范（避免再踩坑）

仓库已按"页面物理隔离"设计，**每人只改自己名下的文件，几乎零冲突**：

| 页面 | 文件 | 负责人 |
|------|------|--------|
| 数据总览 | `static/pages/overview.js` | 吴超 |
| 智能体 | `static/pages/agents.js` | 羽琪 |
| 平台分析(埋点) | `static/pages/tracking.js` | 赵莹 |
| 省份分析 | `static/pages/province.js` | 吴超 |
| 质效分析 | `static/pages/quality.js` | 赵莹 |
| 告警中心 | `static/pages/alarms.js` | 羽琪 |
| 报告生成 | `static/pages/report.js` | 吴超 |
| 内核/配色/导航/数据 | `static/core/*`、`style.v6.css`、`static/index.html` | 仅赵莹 |

协作流程：
1. 三人各自 `git clone` 同一仓库，在 **自己 `pages/<id>.js`** 上改（不要碰 `core/` 与他人文件）。
2. 改完本地自检：`node build/check.js`（全量）或 `node build/check.js static/pages/xxx.js`（单文件）。
3. 提交：`git add static/pages/xxx.js && git commit && git push`。
4. **赵莹**负责 `pull` 合并 → `node build/bundle.js` → 推 `main` → GitHub Pages 自动出新版链接。
5. 数据刷新：赵莹跑取数脚本重写 `static/data.js` 后提交并重新部署（页面只读 `data.js`，不写取数逻辑）。

---

## 六、后续改进建议

1. **把项目挪到永久目录**：当前仓库在 WorkBuddy 会话目录（`/Users/mac/WorkBuddy/2026-09-14-15-53-05/...`）下，会话可能被清理。请先 `cp -r` / `mv` 到稳定位置（如 `~/Projects/lingyun-bi`），再 push。
2. **`data.js`（11MB）用 Git LFS**：避免仓库膨胀；或直接不入库、由 CI 在部署时从金山文档生成（需先接好连接器）。
3. **清理一次性脚本**：仓库里仍保留了 `_cp_weeks.py`、`_proc_*.py`、`_deploy_netlify_api.js`、`app.py` 等个人/遗留脚本，后续可梳理：保留"数据刷新管线"（`kdocs_*`、`xlsx_to_*` 等），删除纯调试脚本。
4. **接管方补数据刷新能力**：若赵莹不在，需有人接手金山文档 kdocs 连接器，否则只能沿用当前 `data.js` 快照（离线可用，但不会更新）。
5. **Token 轮换 + 不在任何文件写密钥**：Netlify 旧 Token 立即吊销；以后统一走环境变量 / GitHub Secrets。

---

## 七、文件位置速查

- 看板本体：`lingyun_dashboard/static/`（双击 `index.html` 即开）
- 生产产物：`lingyun_dashboard/dist/`（已构建好，可直接托管）
- 部署脚本：`lingyun_dashboard/deploy_netlify.sh`（已去 Token，需环境变量）
- 自动部署：`.github/workflows/deploy.yml`（GitHub Pages）
- 协作文档：`lingyun_dashboard/docs/`（决策结论表、多人协作流程、Git 部署方案等）
- 本指南：`lingyun_dashboard/交接与部署指南.md`
