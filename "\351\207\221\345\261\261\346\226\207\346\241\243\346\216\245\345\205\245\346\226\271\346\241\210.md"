# 灵运平台 BI 看板 · 金山文档实时数据接入方案（MVP 评审版）

> 目标：在**固定看板模块不变**的前提下，通过金山文档持续更新底层数据，形成"数据一变、看板与结论同步刷新"的轻量 BI。
> 适用：5 天 MVP、3 名产品人员、第 5 天汇报。

---

## 一、总体思路（一句话）

金山文档只是**数据源**，看板的"壳"（7 个 Tab、图表、下钻、结论逻辑）由代码定死。
**标准模板结构不变 → 模块不变；只把模板里的数字换成金山文档里的最新数字 → 数据更新。**

所以接入的核心不是"做新功能"，而是"把数据读取从本地 CSV 换成金山文档导出的同结构 xlsx"。

---

## 二、两种接入方式（MVP 选哪一种）

| 方式 | 做法 | 实时性 | 是否需要 API/审核 | MVP 推荐 |
|---|---|---|---|---|
| **A. 分享链接 / 本地同步文件** | 在金山文档里"导出 xlsx"或靠 PC 客户端同步到固定路径 `data/live/灵运数据.xlsx`，看板读取该文件，网页点"🔄 刷新"即更新 | 准实时（手动/定时触发，秒级） | 否 | ✅ **MVP 用这个** |
| **B. WPS 开放平台 API** | 注册企业应用、OAuth、调用"获取表格内容"接口，可做到编辑即推送（webhook） | 真·实时 | 是，需企业资质 + 审核周期 | V2 再做 |

**为什么 MVP 选 A 不选 B**：B 需要企业开发者资质、OAuth 授权、接口审核，5 天内产品侧不可控；A 零门槛、今天就能跑通、且完全满足"固定模块 + 数据更新"的诉求。B 作为 V2 演进路线保留。

> 若后续要走 B，只需把 `wps_connector.py` 里的 `load_kdocs()` 换成"调用 API 拉取单元格"的实现，上层渲染逻辑一行都不用改。

---

## 三、你需要知道 / 执行的命令（其实大部分由我来做）

下面的命令列出来是给评审看"技术上要做什么"，**你本人几乎不用敲**：

| 步骤 | 命令 / 操作 | 谁做 | 说明 |
|---|---|---|---|
| 1. 环境（已就绪） | `pip install pandas plotly streamlit openpyxl` | 我（已完成） | openpyxl 用于读 xlsx，已装 |
| 2. 新增连接器 | 把 `wps_connector.py` 放入 `lingyun_dashboard/` | 我 | 负责读 xlsx + 校验结构 |
| 3. 改入口 | `app.py` 侧边栏增加"金山文档（本地同步）"数据源 + "🔄 刷新"按钮 | 我 | 复用现有 7 个 Tab 渲染 |
| 4. 放数据 | 在金山文档导出 xlsx → 存到 `lingyun_dashboard/data/live/灵运数据.xlsx` | 你/运营 | 或金山文档 PC 客户端同步此路径 |
| 5. 启动 | `streamlit run app.py` | 我/你 | 已为你跑起来在 http://localhost:8501 |
| 6. 刷新 | 网页侧边栏点 **🔄 从金山文档刷新** | 你 | 数据一更新，点一下即可 |

**你日常只做两件事**：① 在金山文档里维护标准模板；② 看板页点"刷新"。其余由我和代码兜底。

---

## 四、MVP 验证清单（明细 + 理由）

### 4.1 当前 MVP（v0.5，本地/上传模式）**已验证**

| 编号 | 已验证项 | 验证结果 |
|---|---|---|
| V0 | 样例数据自动加载、7 个 Tab 正常渲染、无报错 | ✅ 本地运行 HTTP 200 |
| V1 | 上传 CSV/Excel → 自动识别数据集 → 看板刷新 | ✅ `detect_dataset()` 按列名识别 6 类表 |
| V2 | 全局时间 / 省份筛选联动 | ✅ 所有 Tab 共用 `weekly_f / province_f` |
| V3 | 一键生成报告（月报 / 领导 / 分中心三种） | ✅ Markdown 导出 |
| V4 | 趋势 / 排行 / 下钻 / 告警模块均出图 | ✅ Plotly 渲染正常 |

### 4.2 接入金山文档后**需新增验证**（核心：固定模块 + 数据更新）

| 编号 | 验证场景 | 具体操作 | 预期结果 | 验证理由（为什么必须验） |
|---|---|---|---|---|
| **N1** | 同源模板数据更新 | 在金山文档改某周 `calls` 值 → 点刷新 | 总览 KPI 与趋势图同步变化，结论文字随新值重算 | **核心诉求**：模块不变、数据实时 |
| **N2** | 同模板另存新文件 | 把同一模板另存为另一份 xlsx 喂给看板 | 同一套模块正常出新数据 | 支持周报轮换 / 多人维护，且不破坏结构 |
| **N3** | 列名被误改 | 把 `calls` 改成 `呼叫量` | 明确提示"缺少必需列：calls"，相关卡片显示"数据异常"而非崩溃 | 协作者改表头是**最高频风险**，必须优雅降级 |
| **N4** | 多出未引用的 sheet | 新增一个看板不读的 sheet 并填数据 | 看板**完全无影响** | 回应"不用的 sheet 修改数据"——验证其安全性 |
| **N5** | 删除必需 sheet | 删掉 `province_weekly` | 提示"缺失数据表：省份周报"，省份 Tab 降级提示，其余正常 | 防止静默空看板误导决策 |
| **N6** | 不同 schema 的 Excel | 上传结构完全不同的 xlsx | **拒绝**，提示"请使用标准模板"，绝不生成异形看板 | 固定模块前提；防止信息架构崩坏（**最大风险**） |
| **N7** | 数值写成文本 / 带逗号 | `1,234` 或 `暂无` | 自动清洗为数值或标黄空值，不中断 | 真实表格常混格式 |
| **N8** | 周数据缺失 | 中间某周空行 | 图表跳过该点并注明，不报错 | 周报偶尔漏填 |
| **N9** | 刷新不闪断 | 点刷新 / 定时触发 | 显示"更新中"，旧数据保留到新数据就绪 | 体验与稳定 |
| **N10** | 结论随数据更新 | 数据变化后看结论卡 | 结论数字与判断随新数据变化（如"Token 增速 X% 高于调用量 Y%"） | "可生成简单结论"诉求 |

> 说明：N1–N10 中，**N3 / N5 / N6 / N7** 是"防崩"类（最关键），**N1 / N2 / N10** 是"核心功能"类，**N4 / N8 / N9** 是"健壮性"类。

---

## 五、对"别人建议"的风险分析与答复

### 建议①：多测试几下
**答复：认同，但要有结构化测试矩阵，而不是随手点。**
- 理由：无矩阵的随意测试容易漏掉 N3（列名误改）、N6（异模板）这类**高风险且难复现**的项。
- 做法：按上面 N1–N10 逐项过，每项记录"操作 / 预期 / 实际 / 是否通过"，形成测试报告附在汇报里。

### 建议②：不用的 sheet 修改数据
**答复：风险低，但前提是"看板只认固定 sheet 名"。**
- 看板只读标准模板里**指定的 6 个 sheet**（weekly_overview / province_weekly / agents / pages / alerts / quality_efficiency），其他 sheet 即使存在也被忽略 → **N4 验证其安全**。
- 风险点只有一个：协作者"以为某 sheet 不用、其实被引用"。兜底办法：把"看板引用的 sheet 清单"写进模板说明页（或模板首页固定一张"字段说明"），公示给所有人。
- 结论：**可以放心在无关 sheet 上改数据**，但要把引用清单公示。

### 建议③：不同的 excel，形成不同的看板
**答复：这与"固定模块不变"直接冲突，MVP 不应支持。**
- 理由 1（前提冲突）：固定模块 = 看板结构由代码定死（总览 / 智能体 / 埋点 / 省份 / 质效 / 告警 / 报告）。若每份 Excel 自动生成不同看板，模块就"变"了，违背你的核心前提。
- 理由 2（结构风险）：协作者随便传表 → 看板天天变样 → 失去"固定模块"的对比价值；且报告生成逻辑依赖固定字段，异形表会让一键报告失效。
- 正确做法（已在 N2 / N6 固化）：
  - **同模板的不同 Excel** → 出**同一套看板**的更新数据（允许，N2 验证）；
  - **结构不符的 Excel** → **拒绝并提示用标准模板**（不允许异形看板，N6 验证）。
- 若业务真需要"多套不同看板"：那是**多模板 / 多实例**（V2+），不在本期 MVP 范围，需单独评审。

---

## 六、边界声明（给协作方看的"红线"）

1. **标准模板 = 6 个固定 sheet + 固定列名**（详见 `wps_connector.py` 的 `EXPECTED_SHEETS`）。
2. **任何数据源**（金山文档 / 上传 / 样例）都必须符合此 schema；结构不符一律拒绝。
3. **看板结构恒定**：只允许数据变，不允许结构变。
4. 数据源切换不影响模块：样例 / 上传 / 金山文档 三者只是"数据从哪来"，渲染完全一致。

---

## 七、下一步

1. 你提供金山文档的**导出文件**或**PC 客户端同步路径**（最稳），我接好 `data/live/`。
2. 我用 N1–N10 跑一轮验证，输出测试报告。
3. （V2）若要做真·实时，再评估 WPS 开放平台 API。

---

*附：当前已落地文件*
- `app.py`：含"金山文档（本地同步）"数据源 + 🔄 刷新按钮
- `wps_connector.py`：读 xlsx + 6-sheet 结构校验 + 类型清洗
- `data/live/`：金山文档导出文件放置目录（放 `灵运数据.xlsx` 即可被读取）
